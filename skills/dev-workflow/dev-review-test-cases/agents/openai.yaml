interface:
  display_name: "独立复核测试清单"
  short_description: "由非清单产出者检查漏项、错项与可执行性"
  default_prompt: "使用 $dev-review-test-cases 绑定完整 (R,S,C,B) 和方案审批，独立复核测试清单的三向追踪、风险覆盖与编码准入；通过后再由编排器构造 T/G，不要执行测试。"
policy:
  allow_implicit_invocation: true
