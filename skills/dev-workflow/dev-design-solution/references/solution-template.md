# <方案名称>

> 状态：Draft / Reviewing · 方案版本：v1 · 设计范围：<系统/模块> · 更新时间：YYYY-MM-DD

> 使用说明：一页结论和变更地图只做导航，不产生正文没有的新事实；现状与改动分开写。详细设计按**机制轴**或**变更轴**展开，二选一，判据见 `solution-structure-contract.md` §2.1——第 4 章给出两套骨架，用哪套删哪套。删除不适用章节，不保留空占位。正文目标不超过约 300 行，长证据放附录，超过约 450 行时拆为独立证据文件。

## 0. 一页结论

### 0.1 要解决的问题

用 3–5 句话说明当前问题、影响和触发原因，不复述完整需求。

### 0.2 核心方案

用 3–5 句话说明关键决策与目标行为，不提前展开实现细节。

### 0.3 变更摘要

| CHG | 动作 | 模块/对象 | 本次调整 | 保持不变 |
|---|---|---|---|---|
| CHG-001 | 新增 / 修改 / 删除 / 保持不变 | | | |

### 0.4 主要收益

- <收益>

### 0.5 主要代价、风险与阻塞项

- 最大代价：
- 最大风险：
- 当前阻塞项：无 / <问题>

## 1. 背景与现状

> 本章只描述已有系统和事实，不讨论目标方案；只保留理解本次决策必需的内容。

### 1.1 当前业务流程

### 1.2 当前存在的问题及证据

### 1.3 为什么现在需要解决

## 2. 目标与非目标

### 2.1 目标与成功标准

| AC | 可验证行为 | 优先级或约束 |
|---|---|---|
| AC-001 | | |

### 2.2 非目标

- <非目标>

### 2.3 已知约束与假设

- 已确认约束：
- 待验证假设：

## 3. 方案总览

### 3.1 设计原则

只列会影响当前设计取舍的原则。

### 3.2 系统边界

| 范围 | 负责内容 | 明确不负责 |
|---|---|---|
| | | |

### 3.3 关键决策

| DEC | 决策 | 核心依据 | 被排除路径 |
|---|---|---|---|
| DEC-001 | | | |

> 没有实质候选取舍时删除“被排除路径”，不制造陪跑方案。

### 3.4 变更地图

| CHG | 动作 | 当前行为 | 调整后行为 | 模块/文件 | 来源 AC/约束 |
|---|---|---|---|---|---|
| CHG-001 | 修改 | | | | AC-001 |

> 本表与详细设计**实际覆盖的行为**必须完全一致；摘要没有、正文新增或只存在于摘要的变更均无效。变更轴下等价于「本表 CHG 集合 == 第 4 章章节集合」；机制轴下本表可只留导航列，完整矩阵放附录 C，并保证每个 CHG 都能指向第 4 章中解释它的小节。

### 3.5 调整后的核心流程

只在跨多个组件、状态或并发时序时放一张主图；节点标记 `[新增]`、`[修改]`、`[删除]` 或 `[保持不变]`，不逐句复述图中内容。

## 4. 详细设计

> 选一种骨架，删掉另一种。不使用“模块改动 / 接口 / 数据 / 并发”作为混排的同级目录。同一事实只定义一次，但允许综合多处事实形成正确性论证。

### 骨架 A：机制轴（核心是一套新机制，正确性依赖多处改动协同）

#### 4.1 核心机制

先用一段话说明机制为什么能成立（问题的本质 → 新机制如何在结构上消除它），再放一张改造前后对比图。不逐句复述图。

#### 4.2 ~ 4.n <机制的组成部分>

按理解顺序分节，每节讲清这一部分承担什么职责、怎么实现、有什么并发契约或误用风险。小节标题用机制语言，不用 `CHG-*` 编号。

#### 4.x 整体正确性论证（涉及并发、时序或一致性时必须有）

按线程或状态视角逐条论证，例如：不会重复触发、不会漏触发、不会提前触发；以及原有保护为什么仍要保留。**这一节是机制轴不可省略的部分**——它是评审判断机制成立与否的唯一入口。

#### 4.y 明确保持不变的行为

列出本次刻意不动的相邻逻辑。机制叙事天然只讲“改了什么”，这一节最容易漏，缺了评审无法判断改动边界。

> `CHG-*` 不出现在本章标题中，改由附录的追踪矩阵承载，并标注每个 CHG 对应的正文位置。

### 骨架 B：变更轴（各变更彼此独立，主要风险在交付边界）

#### 4.1 CHG-001：<变更名称>

- 动作：新增 / 修改 / 删除 / 保持不变
- 来源：AC-001 / 已确认约束 / RISK-001
- 影响模块或文件：
- 关联决策：DEC-001 / 无

##### 当前行为与证据

用最少内容说明现状和问题；详细调用链、日志、SQL 或查询结果移入附录。

##### 本次调整

明确新增、修改或删除什么，不复述完整现状。

##### 调整后行为与不变量

- 目标行为：
- 必须保持的不变量：
- 明确保持不变的相邻逻辑：

##### 失败、降级与相关技术边界

仅保留与本 CHG 相关的接口、数据、配置、异常、并发、幂等、兼容、安全或观测设计；不适用项不创建标题。

##### 影响与验证

- 影响范围：
- 验证方式：
- 预期结果：
- 失败信号：

> 后续 `CHG-002`、`CHG-003` 使用相同结构；多个独立行为不得塞入同一 CHG。

## 5. 备选方案与权衡（仅在存在实质取舍时保留）

| DEC | 方案 | 优点 | 缺点 | 不采用原因 |
|---|---|---|---|---|
| DEC-001 | | | | |

## 6. 评审 FAQ（可选）

预判评审会反复问到的问题，一问一答。用于避免同一问题在评审意见里被问多次；没有可预判问题时删除本章。

## 7. 性能与容量（仅在相关时保留）

| 指标 | 当前值 | 目标/上限 | 估算或证据 | 处置 |
|---|---|---|---|---|
| 调用量 / P90 / P99 / 存储 / 缓存 | | | | |

不得虚构数据；未知但影响决策时列为阻塞或开放问题。

## 8. 验证方案

> 这里只描述方案级验证路径；详细测试检查项由 `dev-derive-test-cases` 在方案评审通过后生成。

| AC | 对应 CHG | 验证方式 | 预期结果 | 失败信号 |
|---|---|---|---|---|
| AC-001 | CHG-001 | | | |

## 9. 发布、观测与回滚

| 阶段 | 涉及 CHG | 前置/动作 | 观测与通过条件 | 回滚条件与步骤 |
|---|---|---|---|---|
| | | | | |

## 10. 风险与待确认事项

| RISK/事项 | 关联 DEC/CHG | 影响 | 缓解或决策条件 | 负责人 | 状态 |
|---|---|---|---|---|---|
| RISK-001 | CHG-001 | | | 待指定 | Open / Pre-authorized |

> 不得虚构负责人或授权。风险预授权最终仍需由方案评审绑定当前 `(R,S,B)`。

## 11. 关联决策（仅在存在 ADR 时保留）

| DEC | ADR 引用 | 关系 |
|---|---|---|
| DEC-001 | | |

## 附录 A：治理身份与版本历史

| 项目 | 内容 |
|---|---|
| 需求身份 `R` | <ref；version；sha256-v1:fingerprint；summary> |
| 代码基线 `B` | <repo-snapshot-v1 身份或 handoff 引用> |
| 方案产出者 | <producer_agent_refs[]；无 agent 参与时为 []> |
| 审批事实 | 不回写本文件；从外部 handoff 的 approval-record-v1 引用读取 |

方案版本大于 v1 时保留变更记录；首次成案删除下表。

| 版本 | 日期 | 触发原因 / finding | 变更摘要 | 上一版归档 |
|---|---|---|---|---|
| v2 | | TR-00X / 重新构思 | | versions/solution-v1.md |

## 附录 B：证据

放置支撑主结论但不适合进入正文的代码锚点、完整调用链、SQL、查询结果、日志和外部资料摘要。正文通过链接或稳定 ID 引用，不复制原文。

## 附录 C：变更清单与追踪矩阵（详细设计用机制轴时保留）

> 正文按机制叙事，本附录提供交付与追踪视图，供 `dev-derive-test-cases` 和评审追踪闭环使用。用变更轴时删除本附录，追踪由第 3.4 节和第 8 章承载。

| CHG | 动作 | 对象 | 调整 | 正文位置 | 来源 AC |
|---|---|---|---|---|---|
| CHG-001 | 新增 / 修改 / 删除 / 保持不变 | | | 4.x | AC-001 |

| AC | 可验证行为 | 覆盖 CHG | 验证入口 |
|---|---|---|---|
| AC-001 | | CHG-001 | 第 8 章 <行> |
