interface:
  display_name: "dev-design-solution"
  short_description: "按场景路由生成简洁、完整且可追踪的技术方案"
  default_prompt: "使用 $dev-design-solution 先判断是否需要正式方案并选择一个主 profile；solution.md 先写相关文档、摘要、主图和关键决策，再按机制解释目标设计；把 CHG 留作交付追踪写进 traceability.md，交付前跑 check_mermaid.js 与读者测试。"
policy:
  allow_implicit_invocation: true
