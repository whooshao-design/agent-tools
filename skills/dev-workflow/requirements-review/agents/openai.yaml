interface:
  display_name: "独立评审需求文档"
  short_description: "由非需求产出者只读评审 PRD、边界与验收标准"
  default_prompt: "使用 $requirements-review 绑定需求身份 R 后按独立评审契约审查这份需求文档，输出唯一结论、分级 findings、对现有验收标准的审查与非规范性建议，以及测试要点。"
policy:
  allow_implicit_invocation: true
