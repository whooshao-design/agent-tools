interface:
  display_name: "Configure Hippo"
  short_description: "安全新增或修改 Hippo 草稿配置，默认预发布且不发布"
  default_prompt: "使用 $configure-hippo 在预发布新增或修改一项 Hippo 草稿配置，保存后回读校验但不要发布。"
policy:
  allow_implicit_invocation: true
