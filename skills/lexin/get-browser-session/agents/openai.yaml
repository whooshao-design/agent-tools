interface:
  display_name: "Get Browser Session"
  short_description: "Check, renew, and reuse WSL browser sessions"
  default_prompt: "Use $get-browser-session to check, renew, or obtain a logged-in browser session for an internal web page."
