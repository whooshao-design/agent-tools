interface:
  display_name: "Query Hippo Config"
  short_description: "快速只读查看 Hippo 应用配置和实际生效配置结果"
  default_prompt: "使用 $query-hippo-config 快速查看 Hippo 配置，默认查询 prod 环境并输出接口路径和结果。"
policy:
  allow_implicit_invocation: true
