interface:
  display_name: "query-dubbo-registry"
  short_description: "通过服务名查提供它的应用名等 Dubbo/FSOF 注册中心只读查询"
  default_prompt: "使用 $query-dubbo-registry 查这个服务是哪个应用提供的"
policy:
  allow_implicit_invocation: true
